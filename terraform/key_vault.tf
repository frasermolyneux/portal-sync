// Key Vault removed — shared secrets (forums API key, map redirect API key)
// are now managed centrally in portal-environments shared Key Vault and
// exposed via Azure App Configuration Key Vault references.
